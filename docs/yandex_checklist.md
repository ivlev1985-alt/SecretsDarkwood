# Yandex Checklist (Этап 7 — релиз-кандидат)

## SDK и платформа
- [x] Структура game_root/ по GDD п.4
- [x] `<script src="/sdk.js">` + fallback `./sdk.js` (stub, no-op при настоящем SDK)
- [x] `YaGames.init()` с таймаутом 4с перед boot (main.js)
- [x] `LoadingAPI.ready()` после загрузки ресурсов (Game.boot)
- [x] Автоязык из `ysdk.environment.i18n.lang` (fallback — настройки/браузер)
- [x] Портретная ориентация (canvas 480x800, fit)
- [x] Управление: WASD/стрелки + мышь + тач-джойстик, без контекстных меню
- [x] Пауза звука при blur/hidden (`pause_on_blur`), freeze кадра при hidden
- [x] Размер архива < 100МБ разархивированного (проверяется перед заливкой, см. Сборка)

## Монетизация (только через SDK, никогда во время боя)
- [x] Interstitial: каждые N смертей (3), min-интервал 60с, пропуск первого забега, показ ДО окна gameover (состояние `ads`)
- [x] Rewarded revive: 1/забег, 50% HP, раздвижка врагов (кнопка блокируется на время видео)
- [x] Rewarded daily-добивка после бесплатного (кулдаун 10 мин)
- [x] Rewarded 📺×2 в меню (+100 монет, кулдаун 10 мин)
- [x] Без своих «рекламных» баннеров — только `showFullscreenAdv/showRewardedVideo`

## Мета и комплаенс
- [x] Сейвы: `player.setData` + localStorage для гостей
- [x] Лидеры: `getLeaderboards` (метрика — лучшее время, сек), локальный fallback
- [x] Авторизация: `openAuthDialog` по кнопке Войти
- [x] Политика/условия: `meta.privacy_url/terms_url` → `ysdk.openUrl` (тап по строке в меню; пустые URL = некликабельно)
- [x] Возрастной рейтинг в `meta.age_rating` (12+)
- [ ] Финальные ассеты (сейчас плейсхолдеры — модерацию не пройдёт)
- [ ] Заливать в НОВЫЙ черновик, следить за рейтингом (не ниже 30)

## Сборка
1. Проверить размер: суммарно < 100МБ.
2. Упаковать содержимое game_root/ (index.html в корне архива) в zip.
3. Загрузить как новый черновик в кабинете Yandex Games.
